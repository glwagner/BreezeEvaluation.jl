# GABLS1 64³ SLD resolution comparison

The domain remains 400 m cubed; spacing halves from 12.5 to 6.25 m. WENO9, physical forcing, rough-wall law, 300 s wall filter, seed 123, γ=2, 300 s SLD filter and factor-one scheme-native resolved transport are unchanged. The bounded three-case registry includes filtered no closure, γ=2 one-face SLD and γ=2 two-face SLD. The first is a numerical-resolution control, one-face compares the same discrete closure at both grids, and two-face keeps the SLD footprint approximately 12.5 m to expose support dependence. No 64³ science case should start before the matched 32³ γ=2 result is admitted and a bounded 64³ H100 gate and throughput benchmark pass.

The 64³ initial potential-temperature perturbations prolong the exact admitted 32³ random realization: each coarse perturbation is replicated to its eight fine cells, while the analytic background is evaluated at fine centers. The fine host-array digest is `1b6c480e8e682c97ade3db99bcf9559cf1723d500858c600b74c318f4a97c864`; the regenerated coarse digest is `1f5db33f2971ee607ac46b1a014b038a09fc876cc1669394dce023a9aa2f198b`. Both are checked by the gate. The runner also saves local similarity functions at both faces for the support-two case.

For comparison, interpolate the 64³ mean u and v to 32³ center heights before computing common-height vector shear; also show each grid native shear. Compare w², w³, resolved/numerical/SGS transport and wall flux over 7–8 and 8–9 h. At z=12.5 m, compare 32³ face one with 64³ face two; show 64³ face one separately at z=6.25 m. A pair of grids establishes a resolution trend, not formal convergence. If the result is ambiguous, a third grid or multiple seeds is needed.
